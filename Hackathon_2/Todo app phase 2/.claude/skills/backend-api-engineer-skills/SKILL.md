Backend API Engineer Skills
Endpoint Creation (FastAPI): Defining async routes (POST /api/chat) and strictly typing Request/Response bodies.

Context Management: Using FastAPI Depends() to inject the Database Session, User Auth context, and Agent Service into the route.

Stateless Architecture: Designing logic that loads history, processes, saves history, and dumps memory in a single request cycle.