# Database Configuration (Required)
DATABASE_URL=postgresql://user:password@host/database

# JWT Authentication (Required)
JWT_SECRET_KEY=your-super-secret-jwt-key-here-min-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Better Auth Integration (Required)
BETTER_AUTH_SECRET=your-better-auth-secret-key-here

# AI/LLM Configuration (Required for chat features)
GEMINI_API_KEY=your-google-gemini-api-key-here

# Application Settings (Optional)
APP_ENV=production
LOG_LEVEL=INFO

# CORS Settings (Optional - configure for your frontend domain)
# ALLOWED_ORIGINS=https://your-frontend-domain.com

# Note: On Hugging Face Spaces, set these as "Secrets" in your Space settings
# Never commit actual secrets to git!
