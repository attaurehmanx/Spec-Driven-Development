MCP Tooling Engineer Skills
Tool Definition (MCP SDK): specific ability to instantiate the MCP Server and register functions using the @mcp.tool() decorator.

Schema Validation (Pydantic): Creating strict input models to sanitize data before it touches the database logic.

CRUD Implementation: Writing the exact Python logic to Add, List, Update, and Delete tasks within the MCP tool context.