AI Systems Architect Skills
LLM Configuration (Gemini/OpenAI Adapter): configuring the OpenAI() client to correctly route requests to Google's Gemini endpoint.

Agent Orchestration: Writing the recursive "Runner Loop" that detects tool_calls, executes them, and feeds the result back to the LLM.

System Prompting: crafting the persona instructions that prevent the AI from accessing other users' data or hallucinating capabilities.