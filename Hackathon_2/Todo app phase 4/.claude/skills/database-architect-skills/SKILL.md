Schema Modeling (SQLModel): Proficient in defining Python classes that map to PostgreSQL tables, specifically Conversation and Message.

Migration Management (Alembic): Capable of generating (alembic revision --autogenerate) and applying (alembic upgrade head) schema changes safely.

Relational Logic: Expert in defining Foreign Keys and indexes to link Users, Tasks, and Conversations without data leaks.