apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "task-management.fullname" . }}-test-connection"
  labels:
    {{- include "task-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "task-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
